# Production Environment Variables (Vercel)
# Copy these to Vercel -> Settings -> Environment Variables

# API URL (Points to your Railway Backend)
NEXT_PUBLIC_API_URL=https://api.team3thesis.dev/api

# Scanner URL (Localhost for Kiosk Mode)
# Keep this as localhost if the scanner is connected to the screen/device running the frontend
NEXT_PUBLIC_SCANNER_URL=http://localhost:5000
NEXT_PUBLIC_SCANNER_PORT=5000

# API Key for Kiosk Mode (Must match BACKEND_API_KEY)
NEXT_PUBLIC_KIOSK_API_KEY=9c719864eb9ae54335201428596bb51bc13b5d817a

# Optional: Google Analytics
# NEXT_PUBLIC_GA_ID=
