release: python download_models.py && python manage.py migrate --noinput && python manage.py collectstatic --noinput
web: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2
# Models will auto-download during release phase
